name: Deploy MkDocs to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

<<<<<<< HEAD
      # 3️⃣ Abhängigkeiten installieren (sauberer, sicherer)
=======
>>>>>>> dcf4258499a31516f8c9b314568674a1e416cae2
      - name: Install MkDocs and Plugins ⚙️
        run: |
          python -m pip install --upgrade pip
          python -m pip install mkdocs mkdocs-material mkdocs-git-revision-date-localized-plugin

<<<<<<< HEAD
      # 4️⃣ Build der statischen Site
=======
      - name: Verify MkDocs Plugin Installation 🧩
        run: |
          python -m pip show mkdocs-git-revision-date-localized-plugin
          mkdocs --version

>>>>>>> dcf4258499a31516f8c9b314568674a1e416cae2
      - name: Build MkDocs Site 🏗️
        run: mkdocs build --clean

<<<<<<< HEAD
      # 5️⃣ Artefakt für GitHub Pages bereitstellen
=======
>>>>>>> dcf4258499a31516f8c9b314568674a1e416cae2
      - name: Upload built site artifact 📦
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4
